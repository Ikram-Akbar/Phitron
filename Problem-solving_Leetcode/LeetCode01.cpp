#include<bits/stdc++.h>
using namespace std;

int main()
{
    //decare an empty array of integers;
    int arr[1000];
    int l,r;
    //take inputs for left and right indices
    cin>>l>>r;
    //take inputs for the array elements
    for(int i=l;i<=r;i++)
        cin>>arr[i];

    // find the minimum subarray based on L and R indices;
    
    return 0;
}