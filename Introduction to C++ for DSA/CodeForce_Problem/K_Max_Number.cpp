#include<bits/stdc++.h>
using namespace std;
void find_max(int arr[], int n)
{
    // use recursion logic here
}

int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i=0; i<n; i++)
    {
        cin>>arr[i];
    }
    find_max(arr,n);
    return 0;
}