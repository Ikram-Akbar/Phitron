#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    long long int x = (long long int)a * b - (long long int)c * d;
    cout << "Difference = " << x << endl;
    return 0;
}
